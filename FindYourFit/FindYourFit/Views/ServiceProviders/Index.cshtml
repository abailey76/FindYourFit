@model List<FindYourFit.Models.ServiceProvider>
<body style="background-color:darkslategray"></body>
<h1 class="text-light">Add Service Provider</h1>

<p>
    <a style="color:lightskyblue" asp-controller="ServiceProviders" asp-action="Add">Add Provider</a>
</p>

<p>
    <a style="color:lightskyblue" asp-controller="ServiceProviders" asp-action="Delete">Delete Provider</a>
</p>

@if (Model.Count == 0)
{
    <p class="text-light">No providers yet!</p>
}
else
{
    <table class="table">
        <tr>
            <th class="text-light">
                Id
            </th>
            <th class="text-light">
                Name
            </th>
            <th class="text-light">
                Description
            </th>
            <th class="text-light">
                Category
            </th>
        </tr>
        @foreach (var evt in Model)
        {
            <tr>
                <td class="text-light">@evt.Id</td>
                <td class="text-light"><a asp-controller="ServiceProviders" asp-action="Detail" asp-route-id="@evt.Id">@evt.Name</a></td>
                <td class="text-light">@evt.Description</td>
                <td class="text-light">@evt.Category.Name</td>
            </tr>
        }
    </table>
}
